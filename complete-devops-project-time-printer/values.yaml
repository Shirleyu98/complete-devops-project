image:
  repository: shirleycutie/complete-devops-project
  tag: 62d705f
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enable: false

serviceAccount:
  enable: false

autoscaling:
  enable: false