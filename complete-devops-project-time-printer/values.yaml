image:
  repository: shirleycutie/complete-devops-project
  tag: 04bfb7e
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enable: false

serviceAccount:
  enable: false

autoscaling:
  enable: false